import "./TitleScroll.scss";
const TitleScroll: React.FC = () => {
  return (
    <div className="container">
      <div className="title">
        DalaNisse — Adam & Albin — Bar Nombre — Operakällaren — Nook — Tabbouli
        —
      </div>
    </div>
  );
};

export default TitleScroll;
